1.php言語でコーディングするよ

2.このファイルはこのパス（住所）に存在するよ

5.6　このパスに存在するRequestとDBをこのファイルで使えるようにするよ

8　AuthorControllerからControllerへ以下の処理を送るよ

10．indexという関数を宣言します。

12.　$items変数を作成。
　　DBにあるauthorsテーブル（表）のデータを丸ごと$itemsに代入するよ

13.index関数をview（ここではblade）に送るよ（使用できるようにするよ）
　　$itemsのままだと変数として送れないから、'items'に$itemsを代入するね

15. addという関数を宣言します。

17. addファイルを表示させるよ（addファイル作ってないけど…どういう意味？？）

19. createという関数を宣言するよ。クライアントが入力した情報を使用するよ

21.～
　　 $paramaという変数を作成して、以下の処理をやってもらうよ
　　	'name''age''nationality'にはそれぞれクライアントが入力したname,age,nationalityを代入するよ

26. DBにあるauthorsテーブル（表）が$paramにアクセスして、中身をテーブルに追加するよ

27．アドレス「/」に追加したデータを更新してね

29.　editという関数を宣言するよ。クライアントが入力した情報を使用するよ

31.  $item変数を作成するよ、
　　　$itemではクライアントが入力したい1件（1レコード？）にクライアントが(idカラムに)入力したidがアクセスして、クライアントに入力したidにDBにあるauthorsテーブル（表）がアクセスするよ

32.　$item変数を使ってedit.blade.phpを表示するよ。
	$itemsのままだと変数として送れないから、'form'に$itemsを代入するね

(省略)

42.　$paramで獲得した連想配列をクライアントが作成したidの中にいれて、DBにあるauthorsテーブル（表）に更新するよ

43.  アドレス「/」にアクセスするよ

（省略）

52.　$param変数を作成するよ	
	クライアントが入力したidを'id'に代入してもらうよ

53.　DBにあるauthorsテーブル（表）のクライアントが入力したidを削除するよ（このときの「->delete()」の「->」はアクセスという意味と違うような気がするのですが？）





　　　